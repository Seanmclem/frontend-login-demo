import React from "react";
import styled from "styled-components";

interface props {}

const Container = styled.div``;

const LoginForm = styled.div``;

export const UserListPage: React.FC<props> = () => {
  return <Container>UserListPage</Container>;
};
